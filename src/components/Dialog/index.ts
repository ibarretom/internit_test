import { Root } from "./Root";
import { Dialog as Element } from "./Dialog";
import { Image } from "./Image";
import { Close } from "./Close";
import { Activator } from "./Activator";

export const Dialog = {
  Root: Root,
  Element: Element,
  Image: Image,
  Activator: Activator,
  Close: Close,
};
